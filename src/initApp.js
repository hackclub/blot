import { render } from './render.tsx'
import { init } from './init.js'


// to initialize dom elements

// for some reason when I use true the default positioning messes up
render()

// to initialize delegation listeners and what not
init()
